services:
  author:
    build: .
    container_name: sob-author
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      - DB_HOST=postgres
      - DB_SSL=false
    restart: unless-stopped
    networks:
      - sob-net

networks:
  sob-net:
    external: true
